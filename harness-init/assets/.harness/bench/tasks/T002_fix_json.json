{
  "id": "T002",
  "name": "修复损坏的 JSON 配置文件",
  "category": "bugfix",
  "difficulty": "easy",
  "description": "项目根目录下有一个 broken_config.json 文件，其中有 JSON 语法错误（缺少逗号、多余的尾逗号等）。请修复这个文件使其成为有效 JSON，并保留所有原始数据。",
  "acceptance_check": "python3 -c \"import json; json.load(open('broken_config.json'))\" 2>/dev/null",
  "expected_files": ["broken_config.json"],
  "max_turns": 5,
  "tags": ["bugfix", "json", "easy"]
}
